import React from 'react';

// eslint-disable-next-line react/prop-types
const User = ({ user }) => {
  
  console.log(user);

  return (
    <div>
        user
    </div>
  );
};

export default User;
